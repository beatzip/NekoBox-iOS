name: CI
on: [push]
jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: xcodebuild -scheme NekoBox-iOS -destination 'platform=iOS Simulator,name=iPhone 15'
